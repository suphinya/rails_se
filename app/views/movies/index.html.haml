%h1 Search TMDb for a movie

= form_tag movies_search_tmdb_path , :method => :get do
  %label Search Terms
  = text_field_tag "search_terms"
  = submit_tag 'Search TMDb'


-#= check_box_tag 'for_kids'
-#= label_tag(:for_kids, "For Kids")

%h1 All Movies

%table#movies
  %thead
    %tr
      %th NO.
      %th Movie Title
      %th Rating
      %th Release Date
      %th More Info
  %tbody

    - @movies.order('title').each_with_index do |movie, index|
      %tr
        %td= index+1
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date.strftime('%d-%b-%Y')
        %td= link_to "More about #{movie.title}", movie_path(movie)
 
= link_to 'Add new movie', new_movie_path       

%br

